#include<bits/stdc++.h>
using namespace std;

int main(){
    int N;
    cin >> N;
    vector<string> names;
    
    for(int i = 0; i < N; i++){
        cin >> names.at(i);
        if(i == N-1) endl;
    }
}